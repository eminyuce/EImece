@model EImece.Domain.Entities.Product
@using EImece.Domain.Helpers.Extensions;
@using EImece.Domain.Helpers;
@using EImece.Domain;
@using Resources;
@{
    var productFiles = Model.ProductFiles;
    var product = Model;
    var categoryName = product.ProductCategory.Name;
    var isUserAuthenticated = User.Identity.IsAuthenticated && AppConfig.IsEditLinkEnable;
    int Width = ViewData["Width"].ToInt(0);
    int Height = ViewData["Height"].ToInt(0);
    var imageId = product.MainImageId.HasValue ? product.MainImageId.Value : 0;
    var imageTag = product.GetCroppedImageTag(imageId, Width, Height, lazy: true, fetchPriority: null, sizes: "80px");
    var productLink = product.DetailPageRelativeUrl;
    var productPriceEnabled = ViewBag.IsProductPriceEnable == null || (bool)ViewBag.IsProductPriceEnable;
}

<div class="media">
    <a class="featured-entry-thumb" href="@productLink" aria-label="@product.Name">@Html.Raw(imageTag)</a>
    <div class="media-body">
        <h3 class="featured-entry-title h6"><a href="@productLink">@product.Name</a></h3>
        <div class="star-rating" aria-label="@product.Rating stars">
            @for (int i = 0; i < product.Rating; i++)
            {
            @:<i class="sr-star active" data-feather="star" aria-hidden="true"></i>
            }
        </div>
        @if (productPriceEnabled)
        {
            <p class="featured-entry-meta">@product.PriceWithDiscount.CurrencySign()</p>
        }
    </div>
</div>
