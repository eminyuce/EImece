@model EImece.Domain.Models.FrontModels.MenuPageViewModel
@using EImece.Domain.Helpers.Extensions
@using Resources
@using System.Linq
@using System.Web.Mvc
@{
    var menu = Model.Menu;
    ViewBag.Title = menu.GetSeoTitle();
    ViewBag.Description = menu.GetSeoDescription();
    ViewBag.Keywords = menu.GetSeoKeywords();
    var files = menu.MenuFiles ?? Enumerable.Empty<EImece.Domain.Models.DTOs.Storefront.StorefrontMenuFileDto>();
    var imgMenuUrl = menu.ImageState ? (menu.GetCroppedImageUrl(menu.MainImageId, 400, 400) ?? "") : "";
    var heroVd = new ViewDataDictionary { { "Alt", menu.Name }, { "Class", "" }, { "PlaceholderClass", "pt-ph pt-ph--square" } };
    var galleryVd = new ViewDataDictionary { { "ColClass", "col-6 col-sm-4 col-md-2" }, { "MinCount", 6 } };
}
<div class="page-title-wrapper" aria-label="Page title">
    <div class="container">
        <div class="alert alert-secondary" role="alert">
            <nav aria-label="breadcrumb">
                <ol class="breadcrumb">
                    <li class="mt-n1 mr-1"><i data-feather="home"></i></li>
                    <li class="breadcrumb-item">
                        <a href="@Url.Action("Index","Home")">@Resource.HomePage</a>
                    </li>
                    <li class="breadcrumb-item active">@menu.Name</li>
                </ol>
            </nav>
        </div>
        <h1 class="page-title">@menu.Name</h1>
        <hr class="mt-4">
    </div>
</div>
<div class="container pt-page pt-t4">
    <div class="pt-float">
        <div class="pt-float__img">
            @Html.Partial("~/Views/Shared/PageThemes/_PageThemeHero.cshtml", (object)imgMenuUrl, heroVd)
        </div>
        <div class="pt-body-text">
            @Html.Raw(menu.Description)
        </div>
    </div>
    @Html.Partial("~/Views/Shared/PageThemes/_PageThemeGallery.cshtml", files, galleryVd)
</div>
