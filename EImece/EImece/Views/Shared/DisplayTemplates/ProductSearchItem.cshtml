@model object
@using EImece.Domain.Models.DTOs.Storefront;
@using EImece.Domain.Entities;
@using EImece.Domain.Helpers.Extensions;
@using EImece.Domain.Helpers;
@{
    var dto = Model as StorefrontProductCardDto;
    var product = Model as Product;
    var name = dto != null ? dto.Name : (product != null ? product.Name : string.Empty);
    var detailUrl = dto != null ? dto.DetailPageRelativeUrl : (product != null ? product.DetailPageRelativeUrl : string.Empty);
    var productCode = dto != null ? dto.ProductCode : (product != null ? product.ProductCode : string.Empty);

    int Width = ViewData["Width"].ToInt(0);
    int Height = ViewData["Height"].ToInt(0);
    var imageTag = dto != null ? dto.GetCroppedImageTag(Width, Height) : (product != null ? product.GetCroppedImageTag(Width, Height) : string.Empty);
}

<div class="four shop columns">
    <figure class="product">
        <div class="mediaholder">
            <a title="@name" href="@detailUrl">
                @Html.Raw(imageTag)
                <div class="cover">
                    @Html.Raw(imageTag)
                </div>
            </a>
        </div>
        <a title="@name" href="@detailUrl">
            <section>
                <h5>@name</h5>
                <span class="product-code">@productCode</span>
            </section>
        </a>
    </figure>
</div>