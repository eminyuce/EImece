@using System;
@using System.Collections;
@using System.Collections.Generic;
@using System.Linq;
@model EImece.Controllers.HomeController.AllCacheList


@{



}
<div class="container">


    <h2>Memory Cache Key: @Model.memoryCacheKey.Count</h2>
    @foreach (var item in Model.memoryCacheKey)
    {
        <div>
            @item
            <hr />
        </div>
    }
    <h2>http Runtime Key: @Model.httpRuntimeKey.Count</h2>
    @foreach (var item in Model.httpRuntimeKey)
    {
        <div>
            @item
            <hr />
        </div>
    }
</div>
