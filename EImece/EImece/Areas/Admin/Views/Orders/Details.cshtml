@using System.Globalization
@using System.Web
@using EImece.Domain
@using EImece.Domain.Entities
@using EImece.Domain.Helpers
@using EImece.Domain.Helpers.Extensions
@using EImece.Domain.Models.Enums
@using Resources

@model Order

@{
    ViewBag.Title = AdminResource.OrderDetails + " - #" + (Model != null ? Model.OrderNumber : "");
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";

    var tr = CultureInfo.GetCultureInfo("tr-TR");
    var orderStatus = (EImeceOrderStatus)Model.OrderStatus;
    var orderStatusList = EImece.Domain.Helpers.EnumHelper.ToSelectList(orderStatus);
    var paymentOk = string.Equals(Model.PaymentStatus, "SUCCESS", StringComparison.OrdinalIgnoreCase);
    var productCount = Model.OrderProducts != null ? Model.OrderProducts.Count : 0;
    var subtotal = Model.Price.ToDecimal() - Model.CargoPrice;
    var couponDiscount = Model.CouponDiscount.ToDecimal();
}

@functions {
    string GetOrderStatusText(EImeceOrderStatus status)
    {
        switch (status)
        {
            case EImeceOrderStatus.NewlyOrder: return AdminResource.OrderStatusNewlyOrder;
            case EImeceOrderStatus.PaymentPending: return AdminResource.OrderStatusPaymentPending;
            case EImeceOrderStatus.InProgress: return AdminResource.OrderStatusInProgress;
            case EImeceOrderStatus.Shipped: return AdminResource.OrderStatusShipped;
            case EImeceOrderStatus.Delivered: return AdminResource.OrderStatusDelivered;
            case EImeceOrderStatus.Cancelled: return AdminResource.OrderStatusCancelled;
            case EImeceOrderStatus.Returned: return AdminResource.OrderStatusReturned;
            case EImeceOrderStatus.Refunded: return AdminResource.OrderStatusRefunded;
            default: return status.ToDescription();
        }
    }
}

@helper OrderStatusLabel(EImeceOrderStatus status)
{
    string css = "text-bg-secondary";
    switch (status)
    {
        case EImeceOrderStatus.NewlyOrder:
        case EImeceOrderStatus.PaymentPending:
            css = "text-bg-warning";
            break;
        case EImeceOrderStatus.InProgress:
            css = "text-bg-info";
            break;
        case EImeceOrderStatus.Shipped:
            css = "text-bg-primary";
            break;
        case EImeceOrderStatus.Delivered:
            css = "text-bg-success";
            break;
        case EImeceOrderStatus.Cancelled:
        case EImeceOrderStatus.Returned:
        case EImeceOrderStatus.Refunded:
            css = "text-bg-danger";
            break;
    }
    <span class="label @css order-status-badge">@GetOrderStatusText(status)</span>
}

@helper PaymentStatusLabel(string paymentStatus)
{
    var ok = string.Equals(paymentStatus, "SUCCESS", StringComparison.OrdinalIgnoreCase);
    var css = ok ? "text-bg-success" : "text-bg-danger";
    string text = paymentStatus;
    if (string.Equals(paymentStatus, "SUCCESS", StringComparison.OrdinalIgnoreCase))
    {
        text = AdminResource.PaymentSuccess;
    }
    else if (string.Equals(paymentStatus, "FAILURE", StringComparison.OrdinalIgnoreCase))
    {
        text = AdminResource.PaymentFailed;
    }
    else if (string.IsNullOrWhiteSpace(paymentStatus))
    {
        text = AdminResource.PaymentNoStatus;
        css = "text-bg-secondary";
    }
    <span class="label @css order-status-badge">@text</span>
}

<div class="order-details-page">
    <div class="clearfix order-details-header">
        <div class="float-end">
            <a href="@Url.Action("Index", "Orders", new { area = "Admin" })" class="btn btn-secondary btn-sm">
                <i class="fa fa-arrow-left"></i> @AdminResource.BackToOrders
            </a>
        </div>
        <h2>
            @AdminResource.OrderDetail
            <small class="text-muted">#@Model.OrderNumber</small>
        </h2>
        <p class="order-details-lead">
            @AdminResource.ViewOrderInfo
        </p>
        <div class="order-details-badges">
            @OrderStatusLabel(orderStatus)
            @PaymentStatusLabel(Model.PaymentStatus)
        </div>
    </div>

    <div class="row order-kpi-row">
        <div class="col-sm-3">
            <div class="order-kpi">
                <div class="order-kpi-label">@AdminResource.OrderDate</div>
                <div class="order-kpi-value">@Model.CreatedDate.ToString("dd.MM.yyyy HH:mm")</div>
            </div>
        </div>
        <div class="col-sm-3">
            <div class="order-kpi">
                <div class="order-kpi-label">@AdminResource.PaidAmount</div>
                <div class="order-kpi-value">@Model.PaidPriceDecimal.CurrencySign()</div>
            </div>
        </div>
        <div class="col-sm-3">
            <div class="order-kpi">
                <div class="order-kpi-label">@AdminResource.PaymentAndCard</div>
                <div class="order-kpi-value">
                    @(string.IsNullOrWhiteSpace(Model.CardAssociation) ? "-" : Model.CardAssociation)
                    @if (!string.IsNullOrWhiteSpace(Model.LastFourDigits))
                    {
                        <small class="text-muted">•••• @Model.LastFourDigits</small>
                    }
                </div>
            </div>
        </div>
        <div class="col-sm-3">
            <div class="order-kpi">
                <div class="order-kpi-label">@AdminResource.Shipment</div>
                <div class="order-kpi-value">
                    @(string.IsNullOrWhiteSpace(Model.ShipmentCompanyName) ? AdminResource.NotAssignedYet : Model.ShipmentCompanyName)
                </div>
            </div>
        </div>
    </div>

    <div class="row">
        <div class="col-md-8">
            <div class="card">
                <div class="card-header"><strong>@AdminResource.CustomerAndAddressInfo</strong></div>
                <div class="card-body">
                    <div class="row">
                        <div class="col-sm-6">
                            <h5 class="order-section-title">@AdminResource.Customer</h5>
                            @if (Model.Customer != null)
                            {
                                <dl class="dl-horizontal order-dl">
                                    <dt>@AdminResource.CustomerFullname</dt>
                                    <dd>@(Model.Customer.FullName ?? "-")</dd>
                                    <dt>@AdminResource.Email</dt>
                                    <dd>
                                        @if (!string.IsNullOrWhiteSpace(Model.Customer.Email))
                                        {
                                            <a href="mailto:@Model.Customer.Email">@Model.Customer.Email</a>
                                        }
                                        else
                                        {
                                            <text>-</text>
                                        }
                                    </dd>
                                    <dt>@AdminResource.Phone</dt>
                                    <dd>@(Model.Customer.GsmNumber ?? "-")</dd>
                                    @if (!string.IsNullOrWhiteSpace(Model.Customer.IdentityNumber))
                                    {
                                        <dt>@AdminResource.TcIdentificationNumber</dt>
                                        <dd>@Model.Customer.IdentityNumber</dd>
                                    }
                                    @if (!string.IsNullOrWhiteSpace(Model.Customer.Company))
                                    {
                                        <dt>@AdminResource.Company</dt>
                                        <dd>@Model.Customer.Company</dd>
                                    }
                                </dl>
                            }
                            else
                            {
                                <p class="text-muted">@AdminResource.NoCustomerRecord</p>
                            }
                        </div>
                        <div class="col-sm-6">
                            <h5 class="order-section-title">@AdminResource.DeliveryAddress</h5>
                            @if (Model.ShippingAddress != null)
                            {
                                <address class="order-address">
                                    @if (!string.IsNullOrWhiteSpace(Model.ShippingAddress.Name))
                                    {
                                        <strong>@Model.ShippingAddress.Name</strong><br />
                                    }
                                    @Model.ShippingAddress.Description<br />
                                    @Model.ShippingAddress.City
                                    @if (!string.IsNullOrWhiteSpace(Model.ShippingAddress.Country))
                                    {
                                        <text>/ @Model.ShippingAddress.Country</text>
                                    }
                                    @if (!string.IsNullOrWhiteSpace(Model.ShippingAddress.ZipCode))
                                    {
                                        <text> @Model.ShippingAddress.ZipCode</text>
                                    }
                                </address>
                            }
                            else
                            {
                                <p class="text-muted">@AdminResource.NoDeliveryAddress</p>
                            }

                            <h5 class="order-section-title">@AdminResource.BillingAddress</h5>
                            @if (Model.BillingAddress != null && Model.ShippingAddress != null && Model.BillingAddress.EqualsAddress(Model.ShippingAddress))
                            {
                                <p class="text-muted">@AdminResource.ShippingAddressSame</p>
                            }
                            else if (Model.BillingAddress != null)
                            {
                                <address class="order-address">
                                    @if (!string.IsNullOrWhiteSpace(Model.BillingAddress.Name))
                                    {
                                        <strong>@Model.BillingAddress.Name</strong><br />
                                    }
                                    @Model.BillingAddress.Description<br />
                                    @Model.BillingAddress.City
                                    @if (!string.IsNullOrWhiteSpace(Model.BillingAddress.Country))
                                    {
                                        <text>/ @Model.BillingAddress.Country</text>
                                    }
                                </address>
                            }
                            else
                            {
                                <p class="text-muted">@AdminResource.NoBillingAddress</p>
                            }
                        </div>
                    </div>
                </div>
            </div>

            <div class="card">
                <div class="card-header">
                    <strong>@AdminResource.OrderProducts</strong>
                    <span class="badge">@productCount</span>
                </div>
                <div class="card-body" style="padding:0;">
                    <div class="table-responsive">
                        <table class="table table-striped table-bordered table-hover mb-0 order-products-table">
                            <thead>
                                <tr>
                                    <th>@AdminResource.Product</th>
                                    <th class="text-center" style="width:80px;">@AdminResource.Quantity</th>
                                    <th class="text-end" style="width:110px;">@AdminResource.UnitPrice</th>
                                    <th class="text-end" style="width:110px;">@AdminResource.Total</th>
                                </tr>
                            </thead>
                            <tbody>
                                @if (Model.OrderProducts != null && Model.OrderProducts.Any())
                                {
                                    foreach (var product in Model.OrderProducts)
                                    {
                                        <tr>
                                            <td>
                                                <strong>@product.ProductName</strong>
                                                @if (!string.IsNullOrWhiteSpace(product.ProductCode))
                                                {
                                                    <br /><small class="text-muted">@AdminResource.Code: @product.ProductCode</small>
                                                }
                                            </td>
                                            <td class="text-center">@product.Quantity</td>
                                            <td class="text-end">@product.ProductSalePrice.CurrencySign()</td>
                                            <td class="text-end"><strong>@product.TotalPrice.CurrencySign()</strong></td>
                                        </tr>
                                    }
                                }
                                else
                                {
                                    <tr>
                                        <td colspan="4" class="text-center text-muted">@AdminResource.NoOrderProducts</td>
                                    </tr>
                                }
                            </tbody>
                            <tfoot>
                                <tr>
                                    <td colspan="3" class="text-end">@AdminResource.SubTotal</td>
                                    <td class="text-end">@subtotal.CurrencySign()</td>
                                </tr>
                                <tr>
                                    <td colspan="3" class="text-end">@AdminResource.Shipment</td>
                                    <td class="text-end">@Model.CargoPrice.CurrencySign()</td>
                                </tr>
                                @if (!string.IsNullOrWhiteSpace(Model.Coupon))
                                {
                                    <tr>
                                        <td colspan="3" class="text-end">@AdminResource.Coupon (@Model.Coupon)</td>
                                        <td class="text-end text-danger">-@couponDiscount.CurrencySign()</td>
                                    </tr>
                                }
                                <tr class="order-total-row">
                                    <td colspan="3" class="text-end"><strong>@AdminResource.TotalPaidPrice</strong></td>
                                    <td class="text-end"><strong>@Model.PaidPriceDecimal.CurrencySign()</strong></td>
                                </tr>
                            </tfoot>
                        </table>
                    </div>
                </div>
            </div>

            <div class="card">
                <div class="card-header">
                    <strong>@AdminResource.PaymentAndCardInfo</strong>
                    <a class="btn btn-link btn-sm float-end" role="button" data-bs-toggle="collapse" href="#paymentTechDetails">@AdminResource.ShowTechDetails</a>
                </div>
                <div class="card-body">
                    <div class="row">
                        <div class="col-sm-6">
                            <dl class="dl-horizontal order-dl">
                                <dt>@AdminResource.PaymentStatusLabel</dt>
                                <dd>@PaymentStatusLabel(Model.PaymentStatus)</dd>
                                <dt>@AdminResource.CardType</dt>
                                <dd>@(Model.CardType ?? "-")</dd>
                                <dt>@AdminResource.CardBrand</dt>
                                <dd>@(Model.CardAssociation ?? "-")</dd>
                                <dt>@AdminResource.CardFamily</dt>
                                <dd>@(Model.CardFamily ?? "-")</dd>
                                <dt>@AdminResource.LastFourDigits</dt>
                                <dd>@(Model.LastFourDigits ?? "-")</dd>
                                @if (!string.IsNullOrWhiteSpace(Model.Installment) && Model.Installment != "1")
                                {
                                    <dt>@AdminResource.Installment</dt>
                                    <dd>@Model.InstallmentDescription</dd>
                                }
                            </dl>
                        </div>
                        <div class="col-sm-6">
                            <dl class="dl-horizontal order-dl">
                                <dt>@AdminResource.PaymentId</dt>
                                <dd><code>@(Model.PaymentId ?? "-")</code></dd>
                                <dt>@AdminResource.Currency</dt>
                                <dd>@(Model.Currency ?? "-")</dd>
                                @if (!string.IsNullOrWhiteSpace(Model.ErrorCode) || !string.IsNullOrWhiteSpace(Model.ErrorMessage))
                                {
                                    <dt>@AdminResource.Error</dt>
                                    <dd class="text-danger">
                                        @Model.ErrorCode
                                        @if (!string.IsNullOrWhiteSpace(Model.ErrorMessage))
                                        {
                                            <br />@Model.ErrorMessage
                                        }
                                    </dd>
                                }
                            </dl>
                        </div>
                    </div>

                    <div id="paymentTechDetails" class="collapse">
                        <hr />
                        <p class="text-muted"><small>@AdminResource.TechDetailsNote</small></p>
                        <div class="row">
                            <div class="col-sm-6">
                                <dl class="dl-horizontal order-dl">
                                    <dt>Conversation</dt>
                                    <dd><code>@(Model.ConversationId ?? "-")</code></dd>
                                    <dt>Sepet ID</dt>
                                    <dd><code>@(Model.BasketId ?? "-")</code></dd>
                                    <dt>Token</dt>
                                    <dd><code>@(Model.Token ?? "-")</code></dd>
                                    <dt>BIN</dt>
                                    <dd>@(Model.BinNumber ?? "-")</dd>
                                </dl>
                            </div>
                            <div class="col-sm-6">
                                <dl class="dl-horizontal order-dl">
                                    <dt>Komisyon (işyeri)</dt>
                                    <dd>@(Model.MerchantCommissionRateAmount.ToDecimal().CurrencySign())</dd>
                                    <dt>Iyzico komisyon</dt>
                                    <dd>@(Model.IyziCommissionRateAmount.ToDecimal().CurrencySign())</dd>
                                    <dt>Iyzico ücret</dt>
                                    <dd>@(Model.IyziCommissionFee.ToDecimal().CurrencySign())</dd>
                                    <dt>Auth / Host</dt>
                                    <dd>@(Model.AuthCode ?? "-") / @(Model.HostReference ?? "-")</dd>
                                </dl>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <div class="col-md-4">
            <div class="card border-primary">
                <div class="card-header"><strong>@AdminResource.OrderManagement</strong></div>
                <div class="card-body">
                    <div class="form-group row mb-3">
                        <label for="orderStatusSelect">@AdminResource.OrderStatusLabel</label>
                        @Html.DropDownList("orderStatusSelectList",
                            new SelectList(orderStatusList, "Value", "Text", orderStatus.ToString()),
                            new { @class = "form-control", id = "orderStatusSelect", @data_change_order_status = Model.Id })
                        <span class="form-text">@AdminResource.StatusChangeAutoSave</span>
                    </div>

                    <div class="form-group row mb-3">
                        <label for="adminOrderNote">@AdminResource.AdminNote</label>
                        <textarea id="adminOrderNote" class="form-control" rows="3"
                                  placeholder="@AdminResource.AdminNotePlaceholder"
                                  data-textarea-admin-order-note="@Model.Id">@Model.AdminOrderNote</textarea>
                    </div>

                    <div class="form-group row mb-3">
                        <label for="shipmentCompany">@AdminResource.ShippingCompany</label>
                        <input id="shipmentCompany" class="form-control" type="text"
                               placeholder="@AdminResource.ShippingCompanyPlaceholder"
                               data-order-shipment-company-name="@Model.Id"
                               value="@Model.ShipmentCompanyName" />
                    </div>

                    <div class="form-group row mb-3">
                        <label for="shipmentTracking">@AdminResource.TrackingNumber</label>
                        <div class="input-group">
                            <input id="shipmentTracking" class="form-control" type="text"
                                   placeholder="@AdminResource.TrackingNumberPlaceholder"
                                   data-order-shipment-tracking-number="@Model.Id"
                                   value="@Model.ShipmentTrackingNumber" />
                            @if (!string.IsNullOrWhiteSpace(Model.ShipmentTrackingNumber))
                            {
                                <span class="input-group">
                                    <a class="btn btn-secondary"
                                       href="@string.Format("{0}/{1}", Constants.ShipmentTrackingCompanyLink, HttpUtility.UrlEncode(Model.ShipmentTrackingNumber.ToStr()))"
                                       target="_blank" title="@AdminResource.OpenTracking">
                                        <i class="fa fa-external-link"></i>
                                    </a>
                                </span>
                            }
                        </div>
                    </div>

                    <button type="button" class="btn btn-success w-100" data-save-admin-order-note="@Model.Id">
                        <i class="fa fa-save"></i> @AdminResource.SaveNoteAndShipment
                    </button>
                    <p class="text-success" style="margin-top:8px;" data-changed-order-result="@Model.Id"></p>
                </div>
            </div>

            <div class="card">
                <div class="card-header"><strong>@AdminResource.CustomerNote</strong></div>
                <div class="card-body">
                    @if (!string.IsNullOrWhiteSpace(Model.OrderComments))
                    {
                        <p style="white-space:pre-wrap; margin:0;">@Model.OrderComments</p>
                    }
                    else
                    {
                        <p class="text-muted" style="margin:0;">@AdminResource.NoCustomerNote</p>
                    }
                </div>
            </div>

            <div class="card">
                <div class="card-header"><strong>@AdminResource.OrderSummary</strong></div>
                <div class="card-body">
                    <dl class="dl-horizontal order-dl">
                        <dt>@AdminResource.OrderNumber</dt>
                        <dd>@Model.OrderNumber</dd>
                        <dt>@AdminResource.CreationDate</dt>
                        <dd>@Model.CreatedDate.ToString("dd.MM.yyyy HH:mm")</dd>
                        <dt>@AdminResource.ProductAmount</dt>
                        <dd>@subtotal.CurrencySign()</dd>
                        <dt>@AdminResource.Shipment</dt>
                        <dd>@Model.CargoPrice.CurrencySign()</dd>
                        <dt>@AdminResource.PaidAmount</dt>
                        <dd><strong>@Model.PaidPriceDecimal.CurrencySign()</strong></dd>
                    </dl>
                </div>
            </div>
        </div>
    </div>
</div>

@section Styles {
    <style>
        .order-details-page { margin-bottom: 24px; }
        .order-details-header { margin-bottom: 16px; padding-bottom: 12px; border-bottom: 1px solid #d9e2ec; }
        .order-details-header h2 { margin-top: 0; margin-bottom: 6px; color: #102a43; font-weight: 600; }
        .order-details-lead { color: #627d98; font-size: 13px; max-width: 720px; margin: 0 0 10px; }
        .order-details-badges .label { margin-right: 6px; font-size: 12px; padding: 5px 10px; }
        .order-kpi-row { margin-bottom: 16px; }
        .order-kpi {
            background: #fff; border: 1px solid #d9e2ec; border-left: 3px solid #62b0e8;
            border-radius: 3px; padding: 12px 14px; min-height: 74px; margin-bottom: 12px;
        }
        .order-kpi-label { color: #627d98; font-size: 11px; font-weight: 600; text-transform: uppercase; margin-bottom: 4px; }
        .order-kpi-value { color: #102a43; font-size: 16px; font-weight: 700; word-break: break-word; }
        .order-section-title { margin-top: 0; margin-bottom: 10px; color: #334e68; font-weight: 600; font-size: 14px; }
        .order-address { margin-bottom: 16px; line-height: 1.45; }
        .order-dl { margin-bottom: 0; }
        .order-dl dt { width: 110px; color: #627d98; font-weight: 600; font-size: 12px; }
        .order-dl dd { margin-left: 120px; margin-bottom: 6px; }
        .order-products-table { margin-bottom: 0; }
        .order-total-row td { background: #f0f4f8; }
        .order-status-badge { display: inline-block; }
        .panel > .card-header { background: #f0f4f8; color: #334e68; }
        .card border-primary > .card-header { background: #334e68; color: #fff; border-color: #334e68; }
    </style>
}
