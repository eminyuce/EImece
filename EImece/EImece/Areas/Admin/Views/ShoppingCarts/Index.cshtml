@model List<ShoppingCart>
@using GridMvc.Html
@using GridMvc.Sorting
@using EImece.Domain;
@using EImece.Domain.Entities;
@using Resources;
@using EImece.Domain.Helpers.Extensions;
@using EImece.Domain.Helpers;

@{
    ViewBag.Title = AdminResource.ShoppingCarts;
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";

    int pageSizeNumber = AppConfig.GridPageSizeNumber;
    int gridPage = Request.QueryString["grid-page"].ToInt();
    var rowIndex = gridPage == 0 ? 0 : AppConfig.GridPageSizeNumber * (gridPage - 1);

    var controllerName = "shoppingcarts";
    var gridName = "ShoppingCartGrid";
    var contents = Model;
}

<h2>@AdminResource.ShoppingCarts</h2>
<hr class="adminLine" />

@Html.Partial("pGridOperations", new ViewDataDictionary(ViewData){
    {"controllerName",controllerName},
    {"orderingButton",false},
    {"StateTypes","N/A" },
    {"IsNewRecordButtonEnabled",false },
    {"selectAll",true},
    {"deleteAll",true},
    {"SetStateOnOrOff",false},
    {"IsExcelButtonEnabled",true}
})

@Html.Partial("Grid/_GridChrome")

<div class="panel panel-primary eg-grid-shell">
    @Html.Grid(contents).Named(gridName).Columns(columns =>
{
    columns.Add()
        .Encoded(false)
        .Sanitized(false)
        .Css("eg-sticky-left eg-sticky-left-1 eg-col-index")
        .RenderValueAs(_ => string.Format("<span class='eg-row-index'>{0}</span>", ++rowIndex));

    columns.Add(o => o.Id, true);

    columns.Add()
        .Encoded(false)
        .Sanitized(false)
        .Css("gridButtons eg-sticky-left eg-sticky-left-2 eg-col-check")
        .RenderValueAs(d => Html.Partial("pGridCheckbox", new ViewDataDictionary(ViewData){
            {"ItemId", d.Id}
        }));

    columns.Add(o => o.Name)
        .Titled(AdminResource.Name)
        .Encoded(false)
        .Sanitized(false)
        .Css("eg-col-name")
        .RenderValueAs(d => Html.Partial("Grid/_GridNameCell", null, new ViewDataDictionary(ViewData) {
            { "Name", d.Name },
            { "ItemId", d.Id.ToString() }
        }));

    columns.Add(o => o.OrderGuid).Titled("Sipariş GUID");
    columns.Add(o => o.UserId).Titled("Kullanıcı ID");

    columns.Add(o => o.IsActive)
        .Titled(AdminResource.IsActive)
        .Sortable(true)
        .Css("smallGridColumn eg-col-status")
        .Encoded(false)
        .Sanitized(false)
        .RenderValueAs(o => Html.Partial("Grid/_GridStatusToggles", null, new ViewDataDictionary(ViewData) {
            { "ItemId", o.Id },
            { "IsActive", o.IsActive },
            { "ShowState", true },
            { "ShowMainPage", false },
            { "ShowImageState", false }
        }));

    columns.Add(o => o.UpdatedDate)
        .Titled(AdminResource.UpdatedDate)
        .Sortable(true)
        .Css("gridDateClass")
        .RenderValueAs(o => o.UpdatedDate.ToShortDateString());

    columns.Add()
        .Titled("İşlemler")
        .Encoded(false)
        .Sanitized(false)
        .Css("eg-sticky-right eg-col-actions")
        .RenderValueAs(d => Html.Partial("Grid/_GridActionsMenu", null, new ViewDataDictionary(ViewData) {
            { "ItemId", d.Id },
            { "ItemName", d.Name },
            { "controllerName", controllerName },
            { "ShowMedia", false },
            { "ShowEdit", false },
            { "ShowDelete", true },
            { "DetailUrl", Url.Action("Detail", controllerName, new { id = d.Id }) },
            { "DetailText", AdminResource.GridDetailText }
        }));

}).WithPaging(pageSizeNumber).Sortable().Filterable()
</div>
